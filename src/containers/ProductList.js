import React from 'react'
import { useSelector } from 'react-redux'

const ProductList = () => {
    // useSelector
  return (
    <div>
        <h1>
            Product List
        </h1>
    </div>
  )
}

export default ProductList